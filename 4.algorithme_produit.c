#include <stdio.h> 

 main(){
     int A,B,P,I;
     printf("Donner deux entiers A et B");
     scanf("%d", &A);
     scanf("%d", &B);

    if (A==0 || B==0){
        P=0
    }
    else P=0
    for (I=1;I=B;I++){
        P=P+A;
    }

    printf("Le produit A*B est : %d",P);
 }